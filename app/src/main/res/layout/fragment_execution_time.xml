<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:gravity="center"
    android:orientation="vertical"
    android:padding="0dp">

    <!-- Caja central (Porcentaje y Estado) -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="2"
        android:gravity="center"
        android:orientation="vertical">

        <!-- Layout para mostrar el porcentaje grande con "%" alineado abajo -->
        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="bottom">

            <TextView
                android:id="@+id/percentage_text"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="0"
                android:textSize="48sp"
                android:textStyle="bold"
                style="@style/percentage"
                android:textColor="@color/violet"
                android:gravity="bottom"/>

            <TextView
                android:id="@+id/percentage_symbol"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="%"
                android:textSize="24sp"
                android:textStyle="bold"
                android:textColor="@color/violet"
                android:gravity="bottom"
                android:layout_marginStart="4dp"/>
        </LinearLayout>

        <!-- Frase debajo del porcentaje -->
        <TextView
            android:id="@+id/status_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Sin cambios"
            android:textSize="14sp"
            android:textStyle="bold"
            android:textColor="@android:color/black"
            style="@style/cuerpobold"
            android:gravity="center"
            android:layout_marginTop="8dp"/>
    </LinearLayout>

    <!-- Imagen inferior -->
    <ImageView
        android:id="@+id/bottom_image"
        android:layout_width="wrap_content"
        android:layout_height="0dp"
        android:layout_weight="2"
        android:src="@drawable/note"
        android:scaleType="fitCenter"/>
</LinearLayout>
