<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:gravity="center"
    android:orientation="vertical"
    android:padding="0dp">

    <!-- Caja central (Puntaje y Estado) -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="2"
        android:gravity="center"
        android:orientation="vertical">

        <!-- Layout para mostrar el puntaje grande con /100 alineado abajo -->
        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="bottom">

            <TextView
                android:id="@+id/score_text"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="0"
                android:textSize="48sp"
                android:textStyle="bold"
                style="@style/percentage"
                android:textColor="@color/violet"
                android:gravity="bottom"/>

            <TextView
                android:id="@+id/score_max_text"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="/100"
                android:textSize="24sp"
                android:textStyle="bold"
                android:textColor="@color/violet"
                android:gravity="bottom"
                android:layout_marginStart="4dp"/>
        </LinearLayout>

        <!-- Frase debajo del puntaje -->
        <TextView
            android:id="@+id/status_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Sin cambios"
            android:textSize="14sp"
            android:textStyle="bold"
            android:textColor="@android:color/black"
            style="@style/cuerpobold"
            android:gravity="center"
            android:layout_marginTop="8dp"/>
    </LinearLayout>

    <!-- Imagen inferior -->
    <ImageView
        android:id="@+id/status_image"
        android:layout_width="wrap_content"
        android:layout_height="0dp"
        android:layout_weight="2"
        android:padding="4dp"
        android:src="@drawable/note"
        android:scaleType="fitCenter"/>
</LinearLayout>
